<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Página Principal - Diagramador de Clases UML</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html, body {
      height: 100%;
      font-family: Arial, sans-serif;
    }

    .container {
      display: flex;
      height: 100vh;
      width: 95vw;
      margin: 0 auto;
    }

    .toolbar {
      display: flex;
      flex-direction: column;
      margin-right: 20px;
      width: 200px;
    }

    .toolbar button {
      padding: 10px 20px;
      margin-bottom: 10px;
      background-color: #4CAF50;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    .toolbar button:hover {
      background-color: #45a049;
    }

    #diagram {
      flex-grow: 1;
      border: 1px solid #ccc;
    }

    body {
      background-color: #f0f2f5;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    h1 {
      text-align: center;
      margin-bottom: 20px;
    }

    /* Menú contextual personalizado */
    #contextMenu {
      display: none;
      position: absolute;
      z-index: 1000;
      background-color: white;
      border: 1px solid #ccc;
      box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2);
    }

    #contextMenu ul {
      list-style: none;
      margin: 0;
      padding: 0;
    }

    #contextMenu ul li {
      padding: 10px 20px;
      cursor: pointer;
    }

    #contextMenu ul li:hover {
      background-color: #f0f0f0;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="toolbar">
      <h1>Diagramador UML</h1>
      <button id="addUserClass">Agregar Clase</button>
      <br><br>
      <button id="addAssociation">Agregar Asociación</button>
      <button id="addDependency">Agregar Dependencia</button>
      <button id="addAggregation">Agregar Agregación</button>
      <button id="addComposition">Agregar Composición</button>
      <button id="addInheritance">Agregar Herencia</button>
      <br><br>
    </div>
    
    <div id="diagram"></div>
  </div>

  <!-- Menú contextual personalizado -->
  <div id="contextMenu">
    <ul>
      <li id="editName">Editar Nombre</li>
      <li id="editAttributes">Editar Atributos</li>
      <li id="editMethods">Editar Métodos</li>
      <li id="deleteClass">Eliminar Clase</li>
    </ul>
  </div>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/lodash.js/4.17.21/lodash.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/backbone.js/1.4.0/backbone-min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jointjs/3.4.2/joint.min.js"></script>
  <script src="/script.js"></script>
</body>
</html>
